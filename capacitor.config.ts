import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mimascotaspa.app',
  appName: 'Mi Mascota Spa',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
